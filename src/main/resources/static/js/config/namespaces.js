'use strict';

/**
 * @namespace Crawler.app.controllers
 * controller module of the app
 */
Crawler.app.controllers = angular.module("Crawler.app.controllers", []);

/**
 * @namespace Crawler.app.services
 * controller module of the app
 */
Crawler.app.services = angular.module("Crawler.app.services", []);

/**
 * @namespace Crawler.app.directives
 * controller module of the app
 */
Crawler.app.directives = angular.module("Crawler.app.directives", []);

/**
 * @namespace Crawler.app.filters
 * controller module of the app
 */
Crawler.app.filters = angular.module("Crawler.app.filters", []);

